function doArea() {
//getting the values from the user what he enters in the textbox on clicking the button//
var radius = document.findarea.rad.value;
// Formula to calculate area of circle//
var result = (3.14 * radius * radius);
//Displaying Area of circle in console//
console.log(result);
}
